# Spam Detector App (FastAPI)

A simple machine learning spam/ham classifier with:

- Scikit-learn ML model
- FastAPI backend
- HTML frontend
- Dockerized for deployment

## Run locally

```bash
uvicorn app.main:app --reload
